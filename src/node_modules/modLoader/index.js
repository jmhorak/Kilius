/**
 * Module loading helper
 * Author: Jeff Horak
 * Date: 7/6/12
 */

exports.load = function(toLoad, modulePath) {
  var module = require(modulePath),
      fn;

  for (fn in module) {
    if (module.hasOwnProperty(fn)) {
      toLoad[fn] = module[fn];
    }
  }

  return toLoad;
}